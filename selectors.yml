selectors:
  - name: commercial_prod
    description: "Commercial domain models for production environment"
    definition:
      union:
        - method: fqn
          value: commerce.intermediate.*
          indirect_selection: buildable
          # parents: true
        - method: fqn
          value: commerce.edw.*
          indirect_selection: buildable
          parents: true

        - method: fqn
          value: commerce.semantic.*
          indirect_selection: buildable
          parents: true
        

