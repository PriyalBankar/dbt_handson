selectors:
  - name: commercial_prod
    description: "Commercial domain models for production environment"
    definition:
      union:
        - method: fqn
          value: commerce.intermediate.*
          indirect_selection: buildable    # ← only run tests when all tested nodes are selected
